# Serverless component

Run this function when a queue ticket is completed. Adapt it to OpenShift Serverless/Knative or the serverless platform supplied by the hackathon. The exact platform YAML depends on the cluster's installed runtime.
